<!-- Loop over each object in the posts array -->
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-12">
    <h2>     
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>    
  <div class="col-md-12">
    <p>
      {{post.content}}
    </p>
  </div>
</div>
{{/each}}